sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t dopehead100/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true dopehead100/react-test npm test

after_success:
  - docker build -t dopehead100/multi-client ./client
  - docker build -t dopehead100/multi-nginx ./nginx
  - docker build -t dopehead100/multi-server ./server
  - docker build -t dopehead100/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push dopehead100/multi-client
  - docker push dopehead100/multi-nginx
  - docker push dopehead100/multi-server
  - docker push dopehead100/multi-worker
